<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fjut.oj.mapper.PermissionMapper">
    <select id="getUserPermission" resultType="com.fjut.oj.pojo.Permission">
        select *
        from userper
        <where>
            <if test="username != null and username != ''">
                and
                username = #{username}
            </if>
        </where>
    </select>
    <select id="getIsAdmin" resultType="java.lang.Boolean">
        SELECT COUNT (*)
        FROM userper
        WHERE username = #{username}
    </select>
</mapper>
